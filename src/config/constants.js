export const tokenDef = Object.freeze({
  user: 'access_token',
  admin: 'admin_token',
  super: 'super_token',
})
export const role = Object.freeze({
  admin: 'admin',
  user: 'user',
  super: 'super',
})
